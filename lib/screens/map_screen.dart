import 'package:flutter/material.dart';

class MapScreen extends StatelessWidget {
  static const routeName = '/maps';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Street Map Screen'),
    );
  }
}
