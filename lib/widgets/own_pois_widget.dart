import 'package:flutter/cupertino.dart';

class OwnPOIWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      key: UniqueKey(),
      child: Text("OwnPOIsHere"),
    );
  }

  OwnPOIWidget();
}
