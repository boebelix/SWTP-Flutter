import 'package:flutter/cupertino.dart';

class OwnGroupWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      key: UniqueKey(),
      child: Text("OwnGroupHere"),
    );
  }

  OwnGroupWidget();
}
